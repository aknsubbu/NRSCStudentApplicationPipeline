# Required API Key for the system
API_KEY=your-secret-api-key-123


# Server URLs
EMAIL_POLLING_URL=http://localhost:8004
DB_SERVER_URL=http://localhost:8000
AI_SERVER_URL=http://localhost:8005
OUTGOING_EMAIL_URL=http://localhost:8001

# MinIO Configuration
BUCKET_NAME=applicationdocs
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=---need to fill---
MINIO_SECRET_KEY=---need to fill---
MINIO_SERVER_URL=http://localhost:8000

# AI Configuration
GEMINI_API_KEY=---need to fill---

# Email Configuration - Incoming (IMAP)
IMAP_SERVER=imap.gmail.com
EMAIL_USERNAME=student@nrsc.gov.in
EMAIL_PASSWORD_IN=---need to fill---
EMAIL_FOLDER=INBOX
PROCESSED_FOLDER=Processed
ATTACHMENT_DIR=attachments
IMAP_TIMEOUT=30
INCLUDE_RAW_EMAIL=false
MARK_AS_READ=true
MOVE_PROCESSED=true

# Email Configuration - Outgoing (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
EMAIL_SENDER=bot.nrsc@gmail.com
EMAIL_PASSWORD_OUT=---need to fill---

# Processing Configuration
EMAIL_POLL_INTERVAL=30
VALIDATION_TIMEOUT=300
MAX_CONCURRENT_VALIDATIONS=5
RETRY_ATTEMPTS=3
MAX_EMAILS=50

# Application Keywords (comma-separated)
APP_KEYWORDS=application,apply,job,position,interview,offer,candidate

# File Configuration
TEMP_DIR=/tmp/app-manager
MAX_FILE_SIZE=52428800
ALLOWED_FILE_TYPES=pdf,doc,docx,jpg,jpeg,png,json

# Monitoring
METRICS_ENABLED=true
LOG_LEVEL=INFO

# Optional
ENCRYPTION_KEY=
POSTGRES_URL=